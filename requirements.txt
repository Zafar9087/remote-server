# ── Server dependencies (deploy on Railway/Render) ────────────────
aiohttp==3.9.5
python-telegram-bot==21.5
websockets==12.0

# ── Client dependencies (install on each PC) ──────────────────────
# pip install websockets pyautogui Pillow psutil requests pyperclip pyinstaller